# CNO symmetry diagnostics

`cno_symmetry.py` one level up is the supported read-only CNO field checker
(with an optional explicitly requested adaptation output).  This directory is
for source-tracing and historical calibration scripts only; they never define
the production regional projector.
